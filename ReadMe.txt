1. Unzip the file test-cassandra-x.x.zip to a directory.
2. Copy the trust store file in the folder 'truststore'
3. Update spark-cassandra.conf with appropriate values
4. Execute run.sh file.
5. Collect the console logs (Check for errors if any) and also share files created in 'logs' folder.